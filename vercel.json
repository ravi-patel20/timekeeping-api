{
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node" }
  ],
  "installCommand": "npm install",
  "routes": [
    { "src": "/", "dest": "/api/health" },
    { "src": "/(.*)", "dest": "/api/$1" }
  ]
}
